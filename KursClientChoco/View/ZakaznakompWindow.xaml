<Window x:Class="KursClientChoco.View.ZakaznakompWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursClientChoco.View"
        xmlns:vm="clr-namespace:KursClientChoco.ViewModel"
        mc:Ignorable="d"
        Title="Добавление автора" Height="450" Width="400"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
          
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Idzakazanapost}" Grid.Row="0"
         Tag=" " Style="{StaticResource PlaceholderTextBoxStyle}"/>
        <TextBox Text="{Binding Idkomp}" Grid.Row="1" Tag=""
         Style="{StaticResource PlaceholderTextBoxStyle}"/>
        <TextBox Text="{Binding Post}" Grid.Row="2" Tag=""
         Style="{StaticResource PlaceholderTextBoxStyle}"/>
        <TextBox Text="{Binding Cinazatovar}" Grid.Row="3" Tag=""
         Style="{StaticResource PlaceholderTextBoxStyle}"/>
        <TextBox Text="{Binding Statyspost}" Grid.Row="4" Tag=""
         Style="{StaticResource PlaceholderTextBoxStyle}"/>
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Content="OK"
            Click="Button_Click" Margin="20 10"/>
            <Button Content="Cancel" Grid.Column="1"
            Click="Button_Click_1" Margin="20 10"/>
        </Grid>
    </Grid>
</Window>
